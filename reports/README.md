# Reports

Generated telemetry reports live here.

- `telemetry_report.png` is the main data visualization report generated by `python visualize.py`.
- `race_*.png` files are per-race exported report images.

The report reads data from `stats/*.csv`.

