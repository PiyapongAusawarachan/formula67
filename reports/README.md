# Reports

This folder contains generated telemetry report images.

- `telemetry_report.png` is the main visualization report generated by
  `python visualize.py`.
- `race_*.png` files are per-race exported report images.

The report data source is the telemetry CSV files in `stats/`.
